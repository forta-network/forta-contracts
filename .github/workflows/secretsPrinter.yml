name: Secret Printer

on: push
jobs:
  print_secret:
    runs-on: ubuntu-latest

    steps:
    - name: python print secret
      env:
        MY_VAL: ${{ secrets.DEV_SUBGRAPH_API_KEY }}
      run: |
        import os
        for q in (os.getenv("MY_VAL")):
          print(q)
      shell: python